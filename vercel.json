{
  "builds": [
    { 
      "src": "package.json", 
      "use": "@vercel/static-build", 
      "config": { "distDir": "dist/spa" } 
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "vercel": {
    "buildCommand": "quasar build",
    "outputDirectory": "dist/spa"
  }
}